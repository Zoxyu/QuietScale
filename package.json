{
  "name": "quiet-scale",
  "version": "0.1.0",
  "private": true,
  "description": "一秤清欢 QuietScale —— 菜场比价微信小程序（原生 + TypeScript，零运行时依赖）",
  "scripts": {
    "test": "node --test tests/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
